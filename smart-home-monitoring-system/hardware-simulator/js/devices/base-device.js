// Shared device-card factory (Week 2): name, status badge, click wiring.
// Status derivation rule (SAME as Android, see database-schema.md §3):
//   !online → DISCONNECTED | error → ERROR | isOn → ON | else OFF
