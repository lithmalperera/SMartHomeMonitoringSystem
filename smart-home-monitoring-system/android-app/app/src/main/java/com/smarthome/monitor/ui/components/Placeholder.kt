package com.smarthome.monitor.ui.components

// ui/components — composables reused by 2+ screens.
// Week 2: DeviceCard, FloorGrid, PowerToggle, StatusBadge, LoadingBox, EmptyState.
// Rule: if a composable is used only once, keep it in its feature folder instead.
