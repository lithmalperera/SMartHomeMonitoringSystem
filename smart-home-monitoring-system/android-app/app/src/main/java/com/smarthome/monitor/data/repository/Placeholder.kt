package com.smarthome.monitor.data.repository

// data/repository — implementations of the domain/repository interfaces.
// Week 1–2: AuthRepositoryImpl, FloorRepositoryImpl, DeviceRepositoryImpl,
// ScheduleRepositoryImpl, UsageRepositoryImpl. Map snapshots → models.
