package com.smarthome.monitor.ui.camera

// ui/camera — CameraScreen + CameraViewModel (route: camera/{deviceId}).
// Week 3: Coil snapshot image, mock stream URL text, connection status badge,
// refresh-snapshot button.
