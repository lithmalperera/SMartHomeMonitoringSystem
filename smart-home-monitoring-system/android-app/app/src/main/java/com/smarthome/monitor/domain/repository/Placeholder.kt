package com.smarthome.monitor.domain.repository

// domain/repository — INTERFACES only (no logic). Frozen on Day 2 so UI and
// data-layer work can proceed in parallel (UI builds against fakes).
// Members: see documentation/architecture.md §8.3.
