package com.smarthome.monitor.ui.device

// ui/device — DeviceControlSheet + DeviceControlViewModel (route: device/{deviceId}).
// Week 2: modal bottom sheet, content by DeviceType (toggle / N gang switches /
// iron countdown + maxActiveMinutes editor / schedule shortcut / open camera).
