package com.smarthome.monitor.ui.home

// ui/home — HomeScreen + HomeViewModel (dashboard).
// Week 2: floor list (name, device count, "N ON"), safety alerts
// ("Iron ON for 20 min"), FAB → Add Floor, entries to Reports/Settings.
