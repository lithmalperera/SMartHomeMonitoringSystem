package com.smarthome.monitor.ui.settings

// ui/settings — SettingsScreen + SettingsViewModel (route: settings).
// Week 3–4: home name, notification toggle (FCM topic), default iron safety
// limit, about.
