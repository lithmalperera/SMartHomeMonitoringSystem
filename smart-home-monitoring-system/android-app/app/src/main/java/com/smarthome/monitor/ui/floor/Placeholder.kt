package com.smarthome.monitor.ui.floor

// ui/floor — FloorScreen + FloorViewModel (route: floor/{floorId}).
// Week 2: grid (gridColumns × gridRows) with device cards at position.x/y,
// status badges, tap → device control sheet, FAB → Add Device dialog.
