package com.smarthome.monitor.ui.navigation

// ui/navigation — Routes.kt (all route strings) + NavGraph.kt (single NavHost).
// Week 1: 8 placeholder screens wired so navigation works end-to-end.
// Routes: splash, home, floor/{floorId}, device/{deviceId}, schedule/{deviceId},
// reports, camera/{deviceId}, settings.
