package com.smarthome.monitor.ui.reports

// ui/reports — ReportsScreen + ReportsViewModel (route: reports).
// Week 4: date selector, per-device active time / sessions / Wh,
// simple bar chart in Compose Canvas (no chart library).
