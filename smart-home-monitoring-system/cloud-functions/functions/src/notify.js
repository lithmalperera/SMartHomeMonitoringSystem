// Shared notification helper (Week 3): publish an FCM message to topic home_{homeId}.
// Used by safetyMonitor (cutoff alerts). Android subscribes to the same topic after
// sign-in — no per-device token storage needed. See documentation/architecture.md §10.4.
